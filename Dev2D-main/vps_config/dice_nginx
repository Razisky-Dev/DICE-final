server {
    listen 80;
    server_name your_domain_or_ip;

    location / {
        include proxy_params;
        proxy_pass http://unix:/var/www/dice_app/dice_app.sock;
    }

    location /static {
        alias /var/www/dice_app/static;
        expires 30d;
    }
}
