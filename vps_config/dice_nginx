server {
    listen 80;
    server_name YOUR_SERVER_IP;

    location / {
        include proxy_params;
        proxy_pass http://127.0.0.1:8000;
    }

    location /static {
        alias /var/www/dice_app/Dev2D-main/static;
        expires 30d;
    }
}
